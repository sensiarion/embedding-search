# Hand-curated (query → expected_file) pairs for this repo.
#
# Used by `cargo xtask golden --corpus benchmarks/golden/this-repo.toml`.
# A query passes if the search result whose `file_path` matches `expected`
# appears within the configured top-k. The harness reports
# (mrr@k, recall@1, recall@5, ndcg@k) per model.
#
# Add new queries whenever a feature lands: each pair anchors a piece of
# the codebase to its conceptual NL phrasing — drift becomes visible
# in the next eval.

[[query]]
text = "where is reranking applied"
expected = "crates/core/src/rerank.rs"

[[query]]
text = "candle metal reranker"
expected = "crates/core/src/candle_rerank.rs"

[[query]]
text = "how is the index incrementally synced"
expected = "crates/core/src/sync.rs"

[[query]]
text = "blake3 chunk hashing"
expected = "crates/core/src/sync.rs"

[[query]]
text = "tree-sitter chunker"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "small chunk concatenation"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "vector index usearch wrapper"
expected = "crates/core/src/vector.rs"

[[query]]
text = "BM25 plus dense reciprocal rank fusion"
expected = "crates/core/src/search.rs"

[[query]]
text = "ONNX runtime embedder backend"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "candle metal embedding encoder"
expected = "crates/core/src/candle_encoder.rs"

[[query]]
text = "model registry built-in list"
expected = "crates/core/src/config.rs"

[[query]]
text = "SQLite chunks file schema"
expected = "crates/core/src/db.rs"

[[query]]
text = "cross-file chunk reuse copy path"
expected = "crates/core/src/sync.rs"

[[query]]
text = "MCP server search tool handler"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "CLI subcommand parsing"
expected = "crates/cli/src/main.rs"

[[query]]
text = "project root resolution and HOME refusal"
expected = "crates/cli/src/root.rs"

[[query]]
text = "agent skill description for semantic search"
expected = "crates/cli/src/SKILL.md"

[[query]]
text = "xtask eval driver markdown report"
expected = "xtask/src/main.rs"

# --- expanded corpus (added 2026-05-26) ---
# Below queries broaden coverage beyond the initial 18: deeper sync,
# embedder, config, search-side, MCP-side, and tooling concepts. They
# stress retrieval against natural-language intent, not symbol lookup.

[[query]]
text = "honor gitignore when walking the project tree"
expected = "crates/core/src/sync.rs"

[[query]]
text = "limit indexing threads based on cpu count"
expected = "crates/core/src/sync.rs"

[[query]]
text = "prepend language hint to chunk before embedding"
expected = "crates/core/src/sync.rs"

[[query]]
text = "rebuild the index when the embedding model changes"
expected = "crates/core/src/sync.rs"

[[query]]
text = "discover sibling git worktrees of this checkout"
expected = "crates/core/src/sync.rs"

[[query]]
text = "delete every index artifact on disk"
expected = "crates/core/src/sync.rs"

[[query]]
text = "progress events emitted during sync"
expected = "crates/core/src/sync.rs"

[[query]]
text = "pick the tree-sitter grammar from file extension"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "pull function or struct name out of the AST"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "enable CoreML execution provider for ONNX"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "fall back to static model2vec embedding"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "remote HTTP embedding service client"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "L2 normalize an embedding"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "reject a decoder model exported with kv cache"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "deterministic stub vector for offline tests"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "load an ONNX model from a user supplied URL"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "estimate peak RAM per model"
expected = "crates/core/src/config.rs"

[[query]]
text = "default directories excluded from the walk"
expected = "crates/core/src/config.rs"

[[query]]
text = "merge user TOML config over defaults"
expected = "crates/core/src/config.rs"

[[query]]
text = "remote provider API key from environment"
expected = "crates/core/src/config.rs"

[[query]]
text = "mean vs cls token pooling enum"
expected = "crates/core/src/config.rs"

[[query]]
text = "schema migration version bump"
expected = "crates/core/src/db.rs"

[[query]]
text = "scope filter results to a subdirectory"
expected = "crates/core/src/search.rs"

[[query]]
text = "lookup enclosing parent chunk for a hit"
expected = "crates/core/src/search.rs"

[[query]]
text = "compute line number from byte offset"
expected = "crates/core/src/search.rs"

[[query]]
text = "warn the agent when the index is still warming up"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "MCP tool to switch the active embedding model"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "register a custom HuggingFace model from the CLI"
expected = "crates/cli/src/main.rs"

[[query]]
text = "verify a model loads end to end before saving"
expected = "crates/cli/src/main.rs"

[[query]]
text = "refuse to index the user home directory"
expected = "crates/cli/src/root.rs"

[[query]]
text = "render a markdown comparison table from eval rows"
expected = "xtask/src/main.rs"

[[query]]
text = "snapshot the source tree into a sandbox for benchmarks"
expected = "xtask/src/main.rs"

# --- second expansion (added 2026-05-26) ---
# Push corpus past statistical noise floor and broaden test/coverage of
# subtle behaviors (neighbor surfacing, model tagging, MCP plumbing).

[[query]]
text = "show prev and next sibling chunks around a hit"
expected = "crates/core/src/search.rs"

[[query]]
text = "rerank fused candidates with cross encoder"
expected = "crates/core/src/search.rs"

[[query]]
text = "BM25 idf and term frequency scoring"
expected = "crates/core/src/search.rs"

[[query]]
text = "tag a model name with its quantization variant"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "filesystem-safe slug for a model cache directory"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "HTTP agent with timeout for HuggingFace downloads"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "parse JSON embeddings from a remote API response"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "decide which ONNX file variant to fetch"
expected = "crates/core/src/config.rs"

[[query]]
text = "BERT versus decoder model architecture enum"
expected = "crates/core/src/config.rs"

[[query]]
text = "normalize a HuggingFace repo identifier string"
expected = "crates/core/src/config.rs"

[[query]]
text = "ONNX model sidecar weights filenames"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "select model subset from a CSV flag"
expected = "xtask/src/main.rs"

[[query]]
text = "MCP server initialize handshake response"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "embed the skill markdown into server instructions"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "wrap a search payload with a warmup hint"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "print index statistics counts and bytes"
expected = "crates/cli/src/main.rs"

[[query]]
text = "remove a registered custom model"
expected = "crates/cli/src/main.rs"

[[query]]
text = "list registered embedding models with RAM estimate"
expected = "crates/cli/src/main.rs"

[[query]]
text = "force a full reindex from scratch"
expected = "crates/cli/src/main.rs"

[[query]]
text = "test that gitignore is honored during the walk"
expected = "crates/core/tests/gitignore.rs"

[[query]]
text = "test that renaming a file reuses its vectors"
expected = "crates/core/tests/reindex.rs"

[[query]]
text = "test that switching models rebuilds the index"
expected = "crates/core/tests/reindex.rs"

[[query]]
text = "synthetic dataset retrieval quality test"
expected = "crates/core/tests/quality.rs"

[[query]]
text = "shared helpers for setting up a temp index in tests"
expected = "crates/core/tests/common/mod.rs"

[[query]]
text = "inspector dumps chunk rows for debugging"
expected = "crates/core/src/inspect.rs"

[[query]]
text = "HNSW search index backed by usearch"
expected = "crates/core/src/vector.rs"

[[query]]
text = "tree-sitter language spec table"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "look up a vector by content hash for cross-file reuse"
expected = "crates/core/src/db.rs"

[[query]]
text = "bump file mtime and size after a no-op resync"
expected = "crates/core/src/db.rs"

[[query]]
text = "dimensions and tokenizer of a registered model"
expected = "crates/core/src/config.rs"

# --- third expansion (added 2026-05-26) ---
# Push past 80 for statistical signal. Add paraphrase variants on hot
# files plus newly-anchored sub-concepts (merkle root, RRF k, sequence
# allocation, attention mask, etc.). Each pair still has a single best
# file — paraphrases differ from earlier text strings.

# sync.rs deeper anchors
[[query]]
text = "merkle hash of the whole indexed tree"
expected = "crates/core/src/sync.rs"

[[query]]
text = "propagate file deletions through the index"
expected = "crates/core/src/sync.rs"

[[query]]
text = "emit a warning when the repo is too large"
expected = "crates/core/src/sync.rs"

[[query]]
text = "two-phase plan and apply for incremental sync"
expected = "crates/core/src/sync.rs"

[[query]]
text = "queue work items between walker and embedder"
expected = "crates/core/src/sync.rs"

[[query]]
text = "include model name in the content fingerprint"
expected = "crates/core/src/sync.rs"

# embedder.rs sub-concepts
[[query]]
text = "dispatch table between static onnx remote candle"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "mean pool over last hidden state"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "max pool variant for sentence embedding"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "feed an attention mask alongside input ids"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "load a user supplied local ONNX file"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "guard against masked language model export"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "warm the embedder with a single throwaway probe"
expected = "crates/core/src/embedder.rs"

[[query]]
text = "embed a batch of strings end to end"
expected = "crates/core/src/embedder.rs"

# search.rs deeper anchors
[[query]]
text = "over-fetch candidates before rerank or scope filter"
expected = "crates/core/src/search.rs"

[[query]]
text = "RRF fusion with k=60 constant"
expected = "crates/core/src/search.rs"

[[query]]
text = "result row with file path range and score"
expected = "crates/core/src/search.rs"

# config.rs deeper anchors
[[query]]
text = "search runtime configuration knobs"
expected = "crates/core/src/config.rs"

[[query]]
text = "cross encoder rerank batch and top n"
expected = "crates/core/src/config.rs"

[[query]]
text = "ONNX execution provider preference list"
expected = "crates/core/src/config.rs"

[[query]]
text = "paths to index database and cache directory"
expected = "crates/core/src/config.rs"

[[query]]
text = "deep merge of user toml over the default"
expected = "crates/core/src/config.rs"

# db.rs deeper anchors
[[query]]
text = "assign the next vector id sequence"
expected = "crates/core/src/db.rs"

[[query]]
text = "replace all chunks for a single file in one transaction"
expected = "crates/core/src/db.rs"

[[query]]
text = "fetch one chunk row by vector id"
expected = "crates/core/src/db.rs"

# chunker.rs deeper anchors
[[query]]
text = "fallback line-based chunking for unknown language"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "concatenate small adjacent AST nodes into one chunk"
expected = "crates/core/src/chunker.rs"

[[query]]
text = "enforce a maximum chunk byte budget"
expected = "crates/core/src/chunker.rs"

# candle paths
[[query]]
text = "load safetensors weights for a candle encoder"
expected = "crates/core/src/candle_encoder.rs"

[[query]]
text = "cross encoder pooler logit head"
expected = "crates/core/src/candle_rerank.rs"

[[query]]
text = "Metal device selection on Apple Silicon"
expected = "crates/core/src/candle_encoder.rs"

# rerank.rs deeper
[[query]]
text = "candle metal versus onnx cpu reranker dispatch"
expected = "crates/core/src/rerank.rs"

[[query]]
text = "score query-document pairs in batches"
expected = "crates/core/src/rerank.rs"

# vector.rs deeper
[[query]]
text = "fetch a stored vector by integer key"
expected = "crates/core/src/vector.rs"

# mcp.rs deeper
[[query]]
text = "expose a tool with an input JSON schema example"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "MCP error mapping helper function"
expected = "crates/cli/src/mcp.rs"

[[query]]
text = "expose set-model tool over MCP"
expected = "crates/cli/src/mcp.rs"

# main.rs deeper
[[query]]
text = "init subcommand creates the per project config"
expected = "crates/cli/src/main.rs"

[[query]]
text = "search subcommand prints results to stdout"
expected = "crates/cli/src/main.rs"

[[query]]
text = "guard a CLI command against indexing the home directory"
expected = "crates/cli/src/main.rs"

[[query]]
text = "warn when a sync ran slower than threshold"
expected = "crates/cli/src/main.rs"

# root.rs deeper
[[query]]
text = "use git rev-parse to find the workspace root"
expected = "crates/cli/src/root.rs"

# xtask
[[query]]
text = "compare every model in the registry against a baseline"
expected = "xtask/src/main.rs"

[[query]]
text = "per-query JSONL record with model and score"
expected = "xtask/src/main.rs"

[[query]]
text = "load TOML corpus file into golden query structs"
expected = "xtask/src/main.rs"

[[query]]
text = "compute MRR Recall NDCG metrics from a results list"
expected = "xtask/src/main.rs"

# tests
[[query]]
text = "test that touching mtime alone does not re-embed"
expected = "crates/core/tests/reindex.rs"

[[query]]
text = "synthetic test fixture for code search ranking"
expected = "crates/core/tests/search_synthetic.rs"

[[query]]
text = "end to end CLI command tests"
expected = "crates/core/tests/core_tests.rs"

# inspect.rs
[[query]]
text = "list every file and its indexed chunk count"
expected = "crates/core/src/inspect.rs"
